
1.
# I have a question in my mind can't i sort the unordered_map ?
# the answer in no because it store in unspecified order 
# untill we need store unodered map into vector then sort based on first value or second value 

# vector<pair<int , int>>
# sort(ump.begin() , ump.end, [](const pair<int , int >& a , cont pair<int , int>& b){
#     # what the basis you want to sort 
#     a.second > b . second # decending order on the basis of 2nd value of map
# });

2 .

# substr(pos, len);
# It returns a string object.


# str1.find(str2) it will find the substring in the string

# s.insert(iterator , character);
# s. insert(position(3 ,4) , string)
# s.erase(iterator1 , iterator2 ) last wala exclude reheta hai 


# want to reinitalize again use 

#  ** fill 

# Dont forget to look the constraints of the question

# Any How try to find your mistake highlights the part that you think here could be the mistake 

# karne se pehle test cases too soch loo warna time bhi barwaad aur question bhi nhi hoga

# To take out the first element of the vector we need to erase that element 

# vector_name.erase(vector_name.begin())


# clarity->testCaseCheck->presenceOfMind->check after complete => always correct code 

<!-- Remember  -->
What is stringstream and what purpose it is used for ?

Easily convert between strings and primitive data types (integers, floats, etc.).

Parsing/Tokenizing: Break a string into individual words or data elements separated by whitespace or a custom delimiter.

like line = "101 dfd 900"
string aur number ko alag karana how 

using stringstream easily can do 


Rules for Recursion : 

    1. Explore everything in terms of i and j.

    2. explore all the paths

    3. max/ min joo bhi operation karna hain 


string(count, char) => made a mistake string(1 , char) => ulta likh diya tha

